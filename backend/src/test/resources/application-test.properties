# Use MySQL for testing
spring.datasource.url=jdbc:mysql://localhost:3306/kudos_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=abc123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Let Hibernate update schema (won’t drop your existing data)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Show SQL in test logs (optional)
spring.jpa.show-sql=true

# Keep SpringDoc off during tests
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
